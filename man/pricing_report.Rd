% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{pricing_report}
\alias{pricing_report}
\title{One-call HTML pricing report}
\usage{
pricing_report(
  model_freq = NULL,
  model_sev = NULL,
  data,
  file = "pricing_report.html",
  title = "GLM Pricing Report",
  variables = NULL,
  include = c("diagnostics", "oneway", "ae", "pdp", "rating"),
  by_year = FALSE,
  exposure_col = "Exposure",
  claims_col = "AantalClaims",
  loss_col = "SCHADELAST",
  year_col = "BOEKJAAR",
  grid_res = 50,
  base_level = c("first", "exposure"),
  trim = c(0, 1)
)
}
\arguments{
\item{model_freq, model_sev}{Fitted glm objects (at least one).}

\item{data}{Dataset with raw rows.}

\item{file}{Output path of the \code{.html} file.}

\item{title}{Report title.}

\item{variables}{Which variables get a section (default: all base
variables of both models).}

\item{include}{Which blocks to render: any of \code{"diagnostics"},
\code{"oneway"}, \code{"ae"}, \code{"pdp"}, \code{"rating"} (default: all).}

\item{by_year}{Split the one-way plots by accounting year.}

\item{exposure_col, claims_col, loss_col, year_col}{Column names, see
\code{\link[=agg_all]{agg_all()}}.}

\item{grid_res, base_level, trim}{Passed to \code{\link[=make_pdp]{make_pdp()}} /
\code{\link[=make_rating_table]{make_rating_table()}}.}
}
\value{
Invisibly, the normalised path of the written file.
}
\description{
Bundles the whole analysis into a single self-browsable HTML report:
model diagnostics (fit table + binned residuals) and, per variable,
the one-way observed plot, actual vs expected, partial dependence and
the rating-factor plot, plus a section with the interaction plots.
Individual plot failures are shown as a note instead of aborting the
report.
}
\details{
Built on \code{htmltools::save_html()} (no pandoc required); the plots are
regular interactive plotly widgets. Next to the \code{.html} file a
\verb{<name>_files/} folder is written with the JavaScript dependencies;
keep the two together when sharing the report.
}
