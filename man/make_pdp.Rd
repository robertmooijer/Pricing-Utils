% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdp.R
\name{make_pdp}
\alias{make_pdp}
\title{Partial dependence plot with observed overlay}
\usage{
make_pdp(
  model,
  raw_data,
  pred_var,
  metric = c("Frequency", "Severity"),
  transform = NULL,
  grid_res = 50,
  y_label = NULL,
  metric_fmt = 4,
  exposure_col = "Exposure",
  claims_col = "AantalClaims",
  loss_col = "SCHADELAST",
  discrete_cutoff = 25,
  y_range = NULL
)
}
\arguments{
\item{model}{A fitted glm object (fitted with a \verb{data =} argument).}

\item{raw_data}{Raw data for the observed line and the bars.}

\item{pred_var}{Name of the predictor (string).}

\item{metric}{\code{"Frequency"} or \code{"Severity"}.}

\item{transform}{Deprecated and ignored (response scale is the default).}

\item{grid_res}{Number of grid points for continuous predictors
(default 50).}

\item{y_label}{Y-axis label (default automatic).}

\item{metric_fmt}{Number of decimals in the tooltip (default 4).}

\item{exposure_col, claims_col, loss_col}{Column names, see \code{\link[=agg_all]{agg_all()}}.}

\item{discrete_cutoff}{See \code{\link[=make_plot]{make_plot()}}.}

\item{y_range}{Optional \code{c(lo, hi)} to fix the primary y-axis range, so
PDPs for different predictors become directly comparable. \code{NULL}
(default) auto-scales to this plot's own values.}
}
\value{
A plotly object with exposure/claim bars, the observed one-way
line and the PDP line.
}
\description{
Computes the PDP on the response scale: for each grid value of \code{pred_var}
the whole training data is set to that value, predicted with
\code{predict(type = "response")}, and the weighted arithmetic mean is taken
(frequency: exposure-weighted; severity: claim-weighted). The offset is
neutralised by setting \code{exposure_col} to 1, so the PDP is a frequency per
unit of exposure and directly comparable to the observed line.
(\code{exp(mean(link))} would be a geometric mean, including the offset, and
systematically biased low.)
}
\details{
The "Observed" line is a one-way marginal (including correlations with
other rating factors); the PDP is a partial effect. Differences between
the two therefore do not necessarily indicate misfit.

Performance: the training data is first collapsed to the unique profiles
of the model's other predictors (with summed weights), so the cost is
\code{grid_res} x (number of unique profiles) predictions instead of
\code{grid_res} x n rows, with an exactly identical result.
}
