% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-oneway.R
\name{make_plot}
\alias{make_plot}
\title{One-way plot of frequency or severity with exposure bars}
\usage{
make_plot(
  data,
  col,
  metric = c("Frequency", "Severity"),
  color_single,
  y_label,
  display = c("color", "facet"),
  by_year,
  metric_fmt = 4,
  exposure_col = "Exposure",
  claims_col = "AantalClaims",
  loss_col = "SCHADELAST",
  year_col = "BOEKJAAR",
  discrete_cutoff = 25
)
}
\arguments{
\item{data}{A data.frame with raw rows.}

\item{col}{Name of the X-axis column (string).}

\item{metric}{\code{"Frequency"} or \code{"Severity"} (computed on the internal
aggregate).}

\item{color_single}{Colour for the metric line when \code{by_year = FALSE}.}

\item{y_label}{Label for the Y-axis.}

\item{display}{\code{"color"} (coloured lines per year) or \code{"facet"}.}

\item{by_year}{\code{TRUE} = split by accounting year.}

\item{metric_fmt}{Number of decimals in the tooltip (default 4).}

\item{exposure_col, claims_col, loss_col, year_col}{Column names, see
\code{\link[=agg_all]{agg_all()}}.}

\item{discrete_cutoff}{Maximum number of unique numeric values that is
still treated as discrete (markers only, default 25).}
}
\value{
A plotly object.
}
\description{
Takes raw policy rows, aggregates them internally via \code{\link[=agg_all]{agg_all()}} and
plots the chosen metric with exposure bars on a secondary axis.
}
\details{
With a character/factor X-axis (or a numeric one with at most
\code{discrete_cutoff} unique values) only markers are shown, so categories are
never connected by a line. Note: in color mode with \code{by_year = TRUE} the
exposure bars show the sum over all years, while the lines are per year.
}
\examples{
\donttest{
d <- data.frame(REGIO = sample(c("N", "Z"), 500, TRUE),
                BOEKJAAR = sample(2023:2024, 500, TRUE),
                Exposure = runif(500, .2, 1),
                AantalClaims = rpois(500, 0.1), SCHADELAST = 0)
make_plot(d, "REGIO", "Frequency", ta_blue, "Frequency",
          display = "color", by_year = TRUE)
}
}
