% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{plot_residual_heatmap}
\alias{plot_residual_heatmap}
\title{Actual-vs-expected heatmap for a pair of variables}
\usage{
plot_residual_heatmap(
  model,
  var_x,
  var_y,
  n_bins = 20,
  min_claims = 30,
  z_range = NULL,
  title = NULL
)
}
\arguments{
\item{model}{A fitted glm object (fitted with a \verb{data =} argument).}

\item{var_x, var_y}{Names of the two variables. They must be columns of
the data the model was fitted on; the values are taken from the
model's own rows, so they always align with the fitted values.}

\item{n_bins}{Maximum number of groups per variable (default 20).}

\item{min_claims}{Cells with fewer claims are shown as blank (default
30).}

\item{z_range}{Optional \code{c(lo, hi)} to fix the colour scale, so several
heatmaps can be compared. \code{NULL} (default) scales to this plot, still
centred on 1.}

\item{title}{Optional plot title.}
}
\value{
A plotly object. The underlying cell table (groups, actual,
expected, A/E, exposure, claims, thin flag) is attached as the
\code{"cells"} attribute.
}
\description{
Shows the exposure-weighted A/E ratio of a fitted GLM per cell of
\code{var_x} x \code{var_y}. This is the check that a one-way plot cannot do: a
missing interaction averages out in the margins (exactly, for a
categorical variable that is in the model) while individual cells can be
badly mispriced.
}
\details{
Both variables are grouped with the same rule used elsewhere in the
package: categorical as is, a numeric variable with at most \code{n_bins}
distinct values on its exact values, otherwise quantile bins.

Cells backed by fewer than \code{min_claims} claims are left blank rather
than coloured, and marked with a grey cross: in a thin cell the A/E is
dominated by noise, and colouring it would be the loudest thing on the
plot. Their volume is still shown in the tooltip.
}
\seealso{
\code{\link[=detect_interactions]{detect_interactions()}} to rank all pairs before plotting one.
}
