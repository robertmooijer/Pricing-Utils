% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{export_rating_table}
\alias{export_rating_table}
\title{Export a rating table to a formatted Excel workbook}
\usage{
export_rating_table(
  rating_tbl,
  file = "rating_table.xlsx",
  overwrite = TRUE,
  digits = 4
)
}
\arguments{
\item{rating_tbl}{Output of \code{\link[=make_rating_table]{make_rating_table()}}.}

\item{file}{Output path of the \code{.xlsx} file.}

\item{overwrite}{Overwrite an existing file (default \code{TRUE}).}

\item{digits}{Number of decimals for the factor columns (default 4).}
}
\value{
Invisibly, the normalised path of the written file.
}
\description{
Writes the \code{\link[=make_rating_table]{make_rating_table()}} output to a formatted \code{.xlsx} file:
an "Overview" sheet with the intercepts (per exposure unit), the base
value per variable and a timestamp; one sheet per main-effect variable
(base row highlighted, thin rows greyed out); and one sheet per
interaction with the long table plus a Level x Group matrix of the
premium factor. Requires the 'openxlsx' package.
}
