% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{glm_collinearity}
\alias{glm_collinearity}
\title{Collinearity between model terms}
\usage{
glm_collinearity(model, threshold = 3)
}
\arguments{
\item{model}{A fitted glm object with at least two terms.}

\item{threshold}{Flag terms whose \code{GVIF_scaled} reaches this value
(default 3).}
}
\value{
A data.frame with \code{Term}, \code{DF}, \code{GVIF}, \code{GVIF_scaled} and
\code{Flag}, ordered by \code{GVIF_scaled}. Returns zero rows with a message
when the model has fewer than two terms.
}
\description{
Reports the generalised variance inflation factor per \strong{term}, not per
coefficient. A plain VIF is meaningless for a pricing model, where a
spline or a factor spans several columns: the generalised form of Fox
and Monette handles multi-column terms, and \code{GVIF^(1/(2*DF))} puts them
back on a scale comparable across terms.
}
\details{
Read \code{GVIF_scaled} like a plain VIF on the square-root scale: the
default threshold of 3 corresponds to a VIF of about 9. A high value
means that term is largely explained by the others, so its coefficient
is unstable and its rating factors are not interpretable on their own,
even though the model's overall predictions may be perfectly fine.

This is the model-level counterpart of the near-duplicate warning in
\code{\link[=screen_features]{screen_features()}}: that one catches correlated \emph{candidates} before
they go in, this one catches them once they are both in the model.
}
\seealso{
\code{\link[=glm_diagnostics]{glm_diagnostics()}}
}
