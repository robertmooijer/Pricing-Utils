% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{detect_interactions}
\alias{detect_interactions}
\title{Rank variable pairs by unexplained interaction structure}
\usage{
detect_interactions(
  model,
  vars = NULL,
  n_bins = 10,
  min_claims = 30,
  n_sim = 200,
  top_n = NULL,
  seed = NULL
)
}
\arguments{
\item{model}{A fitted glm object (fitted with a \verb{data =} argument).}

\item{vars}{Variables to scan. \code{NULL} (default) uses the model's own
predictors (excluding the offset). Variables that are \emph{not} in the
model may be passed too, which is how an entirely omitted rating
factor shows up.}

\item{n_bins}{Maximum number of groups per variable (default 10).}

\item{min_claims}{Cells with fewer claims are ignored when determining
\code{MaxAE} (default 30).}

\item{n_sim}{Simulations for the reference distribution (default 200).}

\item{top_n}{Return only the strongest \code{top_n} pairs (default all).}

\item{seed}{Optional seed, for a reproducible reference distribution.}
}
\value{
A data.frame, strongest first, with one row per pair: \code{VarX},
\code{VarY}, \code{Cells}, \code{Claims}, \code{Deviance}, \code{DF}, \code{Z}, \code{P}, \code{Method},
\code{MaxAE} (A/E of the worst non-thin cell), \code{MaxAE_Claims} and
\code{MaxAE_ExposureShare}.
}
\description{
Scans every two-way combination of \code{vars} and ranks them by how much
cell-level lack of fit remains after the row and column margins have
been matched. The margins are matched by iterative proportional fitting,
so main-effect misfit (a spline that needs another knot, a level that is
simply mispriced) is scaled away first and what remains is genuine
interaction structure.
}
\details{
The statistic is the likelihood-ratio deviance between the additive and
the saturated model on the two-way table of actual versus expected
claims. Because that deviance is unreliable in sparse tables, the
reference distribution is obtained by simulation rather than from the
chi-square asymptotics whenever the model is a Poisson count model:
claims are resampled from the raked expected values under the additive
null and the statistic is recomputed, \code{n_sim} times. For other models
the dispersion-scaled chi-square is used instead (reported in the
\code{Method} column).

The ranking is by statistical signal (\code{Z}), which is not the same thing
as materiality: an effect can be highly significant and commercially
irrelevant, or the reverse. \code{MaxAE} and \code{MaxAE_ExposureShare} are
reported alongside so the two can be judged separately.
}
\seealso{
\code{\link[=plot_residual_heatmap]{plot_residual_heatmap()}} to inspect a pair.
}
