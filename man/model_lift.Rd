% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lift.R
\name{model_lift}
\alias{model_lift}
\title{Lift chart and Gini for a risk premium model}
\usage{
model_lift(
  model_freq = NULL,
  model_sev = NULL,
  data = NULL,
  actual_col = "SCHADELAST",
  exposure_col = "Exposure",
  n_bins = 10,
  y_range = NULL
)
}
\arguments{
\item{model_freq, model_sev}{Fitted glm objects. Supply both for a risk
premium, or one for a frequency- or severity-only lift. Offsets are
neutralised, so the prediction is a rate per unit of exposure.}

\item{data}{Data to evaluate on. \code{NULL} (default) uses the model's own
rows, which is in-sample and is labelled as such on the plot; pass a
holdout for an honest out-of-sample lift.}

\item{actual_col}{Column with the realised amount to compare against:
the loss amount for a risk premium or severity model, the claim count
for a frequency model.}

\item{exposure_col}{Exposure column (default \code{"Exposure"}).}

\item{n_bins}{Number of equal-exposure bins (default 10).}

\item{y_range}{Optional \code{c(lo, hi)} to fix the rate axis.}
}
\value{
A list with \code{table} (one row per bin), \code{gini}, \code{stats}, \code{plot}
(the lift chart) and \code{plot_lorenz} (the curve behind the Gini).
}
\description{
Sorts the portfolio by predicted risk premium, splits it into bins of
equal exposure, and compares the actual with the predicted rate in each.
A model that separates risk produces a rising actual line that tracks the
predicted one; a flat actual line means the model orders nothing.
}
\details{
Bins hold equal \strong{exposure} rather than equal policy counts, so each
point carries the same weight in the comparison, and both series are on
the same scale so they share one axis.

\code{gini} is computed on the exposure-weighted Lorenz curve: cumulative
share of actual losses against cumulative share of exposure, ordered by
the prediction. Zero means the model does not discriminate at all;
higher means it separates risk better. It measures \strong{ordering only} -
a model can have an excellent Gini and still be badly calibrated, which
is what the lift chart itself shows. Note this is not the classical
income Gini, which orders by the variable being measured. A window with
no losses at all has no Lorenz curve to measure, and \code{gini} is then
\code{NA}.

Bins are cut on the ordered predictions, so with fewer distinct
predicted values than bins the policies sharing a prediction are split
across bins in arbitrary order. That is reported with a warning rather
than drawn as if the model had that resolution.
}
\seealso{
\code{\link[=double_lift]{double_lift()}} to compare two candidate tariffs.
}
