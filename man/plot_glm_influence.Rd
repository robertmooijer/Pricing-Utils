% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qq-influence.R
\name{plot_glm_influence}
\alias{plot_glm_influence}
\title{Influence plot: leverage against residual, sized by Cook's distance}
\usage{
plot_glm_influence(
  model,
  n_label = 10,
  data_cols = NULL,
  n_max = 5000,
  max_rows = 2e+05,
  seed = NULL,
  title = NULL,
  y_range = NULL
)
}
\arguments{
\item{model}{A fitted glm object.}

\item{n_label}{Number of most influential rows to highlight and label
(default 10).}

\item{data_cols}{Extra columns from the model's training data to carry
into the tooltip and the returned table. \code{NULL} (default) uses the
model's own base variables.}

\item{n_max}{Plot at most this many ordinary points (default 5000); the
labelled rows are always drawn on top of them.}

\item{max_rows}{Compute on a random sample of at most this many rows
(default 2e5); \code{NULL} uses all of them.}

\item{seed}{Optional seed for that sample.}

\item{title}{Optional plot title.}

\item{y_range}{Optional \code{c(lo, hi)} to fix the residual axis.}
}
\value{
A plotly object, with the \code{n_label} most influential rows
attached as the \code{"influential"} attribute: \code{Row} (row name in the
training data), \code{CooksD}, \code{Leverage}, \code{StdResid}, \code{Actual}, \code{Fitted}
and the \code{data_cols}.
}
\description{
Finds the rows that move the fit, and shows why they do. Leverage on the
x-axis, standardised deviance residual on the y-axis, and Cook's
distance as the marker size: a point is influential because it is
unusual in the predictors (far right), because it is badly fitted (far
up or down), or both.
}
\section{On a large portfolio}{
Cook's distance was designed for regressions with tens of observations,
where a single point can genuinely swing a coefficient. On a book of
100,000 policies no single row does, and every absolute rule of thumb
breaks down: \code{D > 1} never triggers, and \code{D > 4/n} flags thousands of
perfectly ordinary rows. So this plot is deliberately \strong{relative}: it
ranks rows against each other and labels the worst \code{n_label}, rather
than testing them against a threshold that does not apply.

What that ranking is good for is data quality: a vehicle weight of 20
tonnes or an age of 200 lands at the top and is worth opening. It is a
much weaker tool for model structure, where \code{\link[=detect_interactions]{detect_interactions()}} and
\code{\link[=plot_glm_residuals]{plot_glm_residuals()}} have far more power.
}

\section{What it cannot see}{
Influence is not the same as misfit, and on a Poisson frequency model
the two can be opposites. The IRLS weight of a row is its fitted mean,
so a policy with a tiny exposure carries almost no weight in the design
matrix and has almost no leverage - whatever its response. Implant the
classic keying error, an exposure of 0.001 with two claims on it, and
it becomes the \strong{worst-fitted row in the book} (Pearson residual 169,
rank 1 of 20,000) while ranking around 30th on Cook's distance, with a
leverage of 5e-07. Cook's distance structurally cannot see it.

The mirror image is just as awkward: a row with extreme predictors gets
leverage above 0.9, tops the Cook ranking, and has an \emph{unremarkable}
residual - because at that leverage the fit bends to meet it, so the
point masks itself.

That is exactly why this is a two-axis plot rather than an index plot
of \code{D}. Neither axis alone finds both, and a residual check such as
\code{\link[=plot_glm_qq]{plot_glm_qq()}} or \code{\link[=plot_glm_residuals]{plot_glm_residuals()}} is the complement that
catches the low-exposure case.
}

\section{Cost}{
The leverages come from a QR decomposition of the weighted design
matrix, which is the expensive part and grows with rows times
coefficients. \code{max_rows} caps it with a message rather than letting a
large model exhaust memory.
}

\seealso{
\code{\link[=plot_glm_residuals]{plot_glm_residuals()}}, \code{\link[=glm_diagnostics]{glm_diagnostics()}}
}
