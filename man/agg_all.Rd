% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agg.R
\name{agg_all}
\alias{agg_all}
\title{Aggregate raw policy rows to frequency/severity per level}
\usage{
agg_all(
  d,
  col,
  by_year,
  exposure_col = "Exposure",
  claims_col = "AantalClaims",
  loss_col = "SCHADELAST",
  year_col = "BOEKJAAR"
)
}
\arguments{
\item{d}{A data.frame or data.table with raw rows.}

\item{col}{Name of the grouping column (string).}

\item{by_year}{\code{TRUE} = also split by accounting year.}

\item{exposure_col}{Exposure column (default \code{"Exposure"}).}

\item{claims_col}{Claim count column (default \code{"AantalClaims"}).}

\item{loss_col}{Loss amount column (default \code{"SCHADELAST"}).}

\item{year_col}{Accounting-year column (default \code{"BOEKJAAR"}).}
}
\value{
A data.frame with standardised column names: \code{col}, \verb{[Year,]}
\code{Exposure}, \code{ClaimCount}, \code{Loss}, \code{Frequency} (\code{ClaimCount / Exposure},
\code{NA} when exposure is not positive) and \code{Severity}
(\code{Loss / ClaimCount}, \code{NA} when there are no claims).
}
\description{
Goes from raw rows to an aggregate per level of \code{col} (and optionally per
accounting year). This is the aggregation engine used internally by
\code{\link[=make_plot]{make_plot()}}; call it directly when you want the summary table itself.
}
\details{
NA values in the input columns and groups with non-positive exposure are
reported with a warning instead of being silently averaged away.
}
\examples{
d <- data.frame(REGIO = c("N", "N", "Z"), Exposure = c(1, 0.5, 1),
                AantalClaims = c(0, 1, 2), SCHADELAST = c(0, 500, 3000))
agg_all(d, "REGIO", by_year = FALSE)
}
