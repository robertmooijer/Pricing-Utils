% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen.R
\name{screen_features}
\alias{screen_features}
\title{Screen candidate features against a baseline GLM}
\usage{
screen_features(
  model,
  features = NULL,
  split = c(0.6, 0.2, 0.2),
  max_depth = 2,
  eta = 0.05,
  nrounds = 2000,
  early_stopping_rounds = 40,
  n_shap = 4000,
  cor_threshold = 0.95,
  max_levels = 50,
  max_rows = 1e+06,
  nthread = NULL,
  seed = NULL
)
}
\arguments{
\item{model}{Baseline glm, fitted with a \verb{data =} argument. May be as
small as \code{y ~ 1 + offset(log(Exposure))}. Poisson/quasipoisson count
models and Gamma log-link models are supported.}

\item{features}{Candidate columns. \code{NULL} (default) uses every usable
column of the model's data except the response and the offset.}

\item{split}{Row shares for train / validation (early stopping) /
test (reporting). Must sum to 1.}

\item{max_depth}{Maximum tree depth for the second booster (default 2,
i.e. two-way interactions).}

\item{eta, nrounds, early_stopping_rounds}{Boosting parameters.}

\item{n_shap}{Rows sampled for the SHAP interaction ranking (default
4000); \code{0} skips it.}

\item{cor_threshold}{Absolute correlation above which numeric
candidates are reported as near-duplicates (default 0.95).}

\item{max_levels}{Factors with more levels than this are skipped
(default 50).}

\item{max_rows}{Work on a random sample of at most this many rows
(default 1e6); \code{NULL} uses everything. Screening ranks candidates, and
that ranking is stable long before the last million rows are added, so
on a large portfolio this is much the cheapest lever: every stage
(baseline refit, boosting, permutation) scales with the row count. The
reported deviances then refer to the sample.}

\item{nthread}{Threads for boosting. \code{NULL} (default) uses one fewer
than the available cores, capped by \code{OMP_THREAD_LIMIT} and reduced to
2 under \verb{R CMD check}, which limits what a package may claim. Boosting
dominates the runtime, so this is the second lever: on an eight-core
machine the measured gain over a single-threaded run is more than a
factor of two.}

\item{seed}{Optional seed for the sample, the split and the boosting.}
}
\value{
A list with \code{summary} (the staged deviance comparison),
\code{verdict} (one sentence), \code{features} (ranked candidates), \code{gain_note},
\code{interactions} (SHAP ranking, or \code{NULL}), \code{correlated} (near-duplicate
pairs, or \code{NULL}), \code{stats} (the raw numbers) and \code{plot} (ranked
incremental importance). Requires the 'xgboost' package.
}
\description{
Fits a gradient boosting model with the GLM as an offset, so the booster
can only explain what the GLM leaves behind. Everything it returns is
therefore a statement about the GLM, not a pricing model: no scorable
object is returned.
}
\details{
It answers two questions, depending on the baseline you pass:
with a minimal \code{model} it screens candidate features \emph{before} you choose
what goes into the tariff ("what does this data add on top of what I
already price on"); with your full model it asks what the finished
tariff still misses.
}
\section{Reading the output}{
\code{summary} compares out-of-sample deviance for the baseline, a depth-1
booster (additive corrections only) and a depth-\code{max_depth} booster
(which may use interactions). The verdict is deliberately staged: while
the depth-1 step still improves the fit, main effects are missing and
the interaction ranking cannot be trusted yet. Fix the main effects,
then run again.

\code{features} is sorted by \code{PermDeviance}: the increase in out-of-sample
deviance when that feature is shuffled, with the baseline held fixed, so
it measures the feature's \emph{incremental} contribution.

\strong{Read it as a ranking, not as a test.} Zero is not a threshold. The
figure is a difference between two deviances on one random split, so it
carries real sampling noise, and its scale depends on how large that
split is - a value of 5 means nothing on its own. Measured on a
150,000-row book over three seeds, pure noise columns ranged from -3.8
to +2.3 while a genuine but weak predictor ranged from -1.9 to +2.0.
The strong features were never in doubt (30 to 48); the marginal ones
were not separable at all.

Two cheap ways to see where the noise band sits. Add a column you know
is noise - \code{data$CONTROL <- rnorm(nrow(data))} - and read every other
feature against where it lands. And re-run with two or three \code{seed}
values: anything whose sign flips between runs is inside the band.

\code{Gain} is reported alongside but is unreliable on its own: it is biased
towards continuous and high-cardinality features, which routinely gives
pure noise a respectable-looking share.

Correlated candidates split their importance between them, so a pair of
near-duplicates can both look mediocre. Pairs above \code{cor_threshold} are
reported in \code{correlated}.
}

\section{Sensitivity}{
A booster is \emph{less} sensitive to a two-way interaction between known
rating factors than \code{\link[=detect_interactions]{detect_interactions()}} is: the cell-based test
concentrates the signal into one statistic with a known null, while the
booster has to discover the split structure and pays a variance cost for
its flexibility. Use this function to screen features and to get the
global verdict; use \code{\link[=detect_interactions]{detect_interactions()}} to hunt for interactions.
}

\seealso{
\code{\link[=detect_interactions]{detect_interactions()}}, \code{\link[=plot_residual_heatmap]{plot_residual_heatmap()}}
}
